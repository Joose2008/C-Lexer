/* ejemplo3.c - Identificadores, numeros y maximal munch */
int main(void) {
    int _temp = 0;
    int contador123 = 007;
    int MiVariable = 1024;
    int x = 1;
    /* maximal munch: compuestos vs simples adyacentes */
    if (x == 1) { x = 2; }
    if (x != 1) { x = 3; }
    if (x <= 1) { x = 4; }
    if (x >= 1) { x = 5; }
    if (x < 1) { x = 6; }
    if (x > 1) { x = 7; }
    if (x && 1 || x & 1 | 0) { x = 8; }
    x = x << 2;
    x = x >> 2;
    x = x + y - z * w / q;
    x = -y;
    x = !y;
    x = ~y;
    x = y ^ z;
    int a, b;
    int t: ;
    a = (b);
    arr[0] = 1;
    p.x = 1;
    ptr->x = 2;
    {
    }
    return 0;
}
